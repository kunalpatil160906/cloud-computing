name: CI
on: push
jobs:
  deploy:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/master'
    steps:
     - uses: actions/checkout@v4
     - run : echo "depolying, because this is main"
 
