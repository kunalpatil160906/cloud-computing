name: CI
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run : echo "running tests"
  deploy:
     needs: test 
     runs-on: ubuntu-latest
     steps:
      - uses: actions/chakeout@v4
      - run: echo "depoying to production"
